import "../App.css";

const ProfileHeader = () => {
  return (
    <div className="userLinksContainer">
      <span className="userLinks">Profile</span>
      <span className="userLinks">Orders</span>
      <span className="userLinks">Favorites</span>
    </div>
  );
};

export default ProfileHeader;
